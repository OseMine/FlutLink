# Copy this file to `.env` and fill in the value (`.env` is gitignored).
# FlutLink is a dedicated client for this one FlutCloud server. The URL is
# only read from `FLUTCLOUD_URL` and is never hard-coded in the repository.
# CI release builds additionally bake it into the binaries at compile time.
FLUTCLOUD_URL=
