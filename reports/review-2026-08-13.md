# Code-Review FlutLink — 2026-08-13

**Reviewer:** opencode (Agent `reviewer`, Model deepseek-v4-flash-free)
**Datum:** 13. August 2026
**Fokusbereich:** gesamtes Projekt
**Geprüfte Dateien:** README.md, package.json, vite.config.ts, tsconfig*.json,
`.env`, `src/` (alle Komponenten, Stores, `lib/ipc.ts`, `lib/i18n.ts`,
`lib/format.ts`), `src-tauri/` (lib.rs, main.rs, commands.rs, accounts.rs,
state.rs, error.rs, sync.rs, nextcloud/mod.rs, nextcloud/webdav.rs,
nextcloud/ocs.rs, tauri.conf.json, capabilities/), `.github/` (alle
Workflows, Actions, dependabot.yml, ISSUE_TEMPLATE), `.opencode/`,
`docs/README.md`, `AGENTS.md`. `todo.md` ist leer (0 Bytes), keine
`archived-todo.md` vorhanden.

## Verifikation (ausgeführt)

| Check | Ergebnis |
| --- | --- |
| `cargo test --manifest-path src-tauri/Cargo.toml` | ✅ 21 Tests bestanden |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | ✅ keine Warnungen |
| `npm run build` (vue-tsc + vite) | ✅ kompiliert sauber |

Zusätzlich wurde ein reproduzierbarer Logik-Test für den
`account_add`-Re-Add-Bug in `/tmp` erstellt und ausgeführt (Ergebnis:
**Bug bestätigt**, siehe Finding B1).

---

## Befunde

### B1 — Bug: Erneutes Hinzufügen des aktiven Kontos schaltet still auf ein anderes Konto um

**Datei/Funktion:** `src-tauri/src/commands.rs` → `account_add` (Zeilen 47–61) i. V. m. `src-tauri/src/state.rs` → `upsert` (Zeilen 148–156) und `current` (Zeilen 138–145)

`has_accounts` wird **vor** dem `upsert` aus der Snapshot-Liste berechnet. Wenn
ein bereits vorhandenes Konto erneut hinzugefügt wird (z. B. Token-Erneuerung),
gilt `has_accounts == true` → `is_active: false`. `upsert` entfernt danach den
bestehenden Eintrag (gleiche `instance_url` + `username`) und hängt den neuen
mit `is_active=false` an. Ergebnis: **kein** Konto ist mehr als aktiv markiert,
und `current()` fällt auf `guard.first()` zurück — das kann ein *anderes*
Konto sein. Der Nutzer denkt, er erneuert sein aktives Konto, und die App
arbeitet still auf einem anderen Konto weiter.

Verifiziert mit Replikation der exakten Logik (siehe Abschnitt „Verifikation"):

```
snapshot after re-adding account A:
  B active=false
  A active=false
current() => B
RESULT: BUG CONFIRMED — active account silently switched to B
```

**Fix-Vorschlag:** `is_active` nach dem `upsert` bestimmen (bzw. `upsert`
so ändern, dass ein ersetztes Konto den `is_active`-Status des entfernten
Eintrags übernimmt, falls kein anderes aktiv ist).

---

### B2 — Bug: Theme „System Default" folgt nie dem Betriebssystem

**Datei/Funktion:** `src/App.vue` → `resolveTheme` (Zeilen 37–39) und der
`matchMedia`-Listener (Zeilen 49–51)

```ts
function resolveTheme() {
  resolvedTheme.value = ui.theme === "system" ? "operationflut" : ui.theme;
}
```

`"system"` wird **immer** auf `"operationflut"` abgebildet, nie auf
`"midnight"`. Der Listener lauscht zwar auf `prefers-color-scheme: light`,
das Ergebnis wird aber ignoriert. Der i18n-Text `systemThemeNote`
(„System Default follows your operating system") verspricht mehr, als der
Code liefert. Ein Nutzer, der „System Default" wählt, bekommt immer das
OperationFlut-Theme.

**Fix-Vorschlag:** `matchMedia("(prefers-color-scheme: dark)").matches`
auswerten und bei `system` auf `"midnight"` abbilden, wenn das OS dunkel
präferiert (bzw. Option entfernen, falls bewusst nur dunkel).

---

### B3 — Feature-Lücke: Share-Erstellung während Admin-Impersonation zielt auf das falsche Konto

**Datei/Funktion:** `src-tauri/src/commands.rs` → `webdav_create_share` (Zeilen 264–268), `src-tauri/src/nextcloud/ocs.rs` → `create_share` (Zeilen 231–251), `src/components/FileExplorer.vue` → `createLink` (Zeilen 31–41)

`webdav_create_share` nimmt kein `target_user`. Wenn ein Admin im
Impersonations-Modus (Browsing der Dateien von Benutzer B) einen Link
erstellt, wird `entry.path` (relativ zu B's Dateiroot) gegen das **Admin**-Konto
an die OCS-Share-API geschickt. Das ergibt entweder einen 404/Fehler oder —
schlimmer — einen falschen Link auf einen gleichnamigen Pfad im Admin-Account.
`webdav_list` unterstützt `target_user` bereits, `webdav_create_share` nicht.

**Fix-Vorschlag:** `target_user` als Parameter ergänzen und beim Share-Aufruf
die Impersonation (bzw. den richtigen Account) verwenden.

---

### B4 — Feature-Lücke: Benutzerliste ist hart auf 200 limitiert (keine Pagination)

**Datei/Funktion:** `src-tauri/src/nextcloud/ocs.rs` → `list_users` (Zeile 79)

`limit=200` ist fest verdrahtet; Benutzer jenseits der 200 werden nie
geladen. Die OCS-API unterstützt `offset` für Pagination. Bei Instanzen mit
> 200 Benutzern fehlen Einträge im Admin-Panel und in der Impersonation-Auswahl.

---

### B5 — Bug: `ocs_current_user`-Command ist tot (kein IPC-Wrapper, keine Frontend-Nutzung)

**Datei/Funktion:** `src-tauri/src/commands.rs` → `ocs_current_user` (Zeilen 353–357), `src-tauri/src/lib.rs` → Zeile 155, `src/lib/ipc.ts` (gesamte Datei)

Der Command ist registriert und implementiert, aber `ipc.ts` enthält keinen
Wrapper und kein Frontend-Code ruft ihn auf. Entweder im Frontend nutzen
(z. B. für die Account-Details-Anzeige) oder aus dem Handler entfernen.

---

### B6 — Bug: Event `sync-folders-changed` (CLI `--path`) wird von niemandem empfangen

**Datei/Funktion:** `src-tauri/src/lib.rs` → `handle_cli` (Zeile 92), `src/stores/sync.ts` (bind/load)

Bei `flutlink --path <dir>` wird nach erfolgreichem `sync_add` das Event
`sync-folders-changed` emittiert — kein `listen` im Frontend registriert es.
Das Sync-Panel zeigt den neuen Ordner erst nach erneutem Öffnen (load on
mount). Entweder auf das Event hören und `sync.load()` aufrufen oder das
Event entfernen.

---

### B7 — Bug: `resolveTheme`-Bug-Pendant — README/Doku überzeichnet „change notifications"

**Datei/Funktion:** `README.md` (Zeile 63), `src-tauri/Cargo.toml` (Dependencies), `src-tauri/src/sync.rs` → `spawn_worker` (Zeilen 852–866)

Der README behauptet „the background worker (10 s interval + change
notifications)". Im Code gibt es **keinen** Dateisystem-Watcher (keine
`notify`-Crate; nur `tokio::sync::Notify` für manuelle Trigger + 10-s-Polling).
Doku anpassen oder echten Watcher ergänzen (Feature).

---

### B8 — Bug (Minor): Nicht-aktives Konto nach Re-Add wird in der UI nicht korrekt dargestellt

**Datei/Funktion:** `src/stores/accounts.ts` → `add` (Zeilen 47–61) und `load` (Zeilen 21–33)

In Kombination mit B1: `add()` pusht das Konto nur, wenn der Username neu ist
(`if (!accounts.value.some(...))`), und setzt `active` nur bei
`account.isActive`. Nach B1-Szenario bleibt das alte `active`-Objekt stehen,
während das Backend ein anderes Konto liefert — Store und Backend laufen
auseinander. B1 beheben; danach `add()` um einen Reload der Account-Liste
ergänzen.

---

### B9 — Robustheit: WebDAV-Liste erkennt stilles Ignorieren der Impersonation nicht

**Datei/Funktion:** `src-tauri/src/nextcloud/webdav.rs` → `list` (Zeilen 39–41, 46–50) und `relative_path` (Zeilen 372–383)

Wenn die `Impersonate-User`-Unterstützung auf dem Server fehlt (App
`impersonate` nicht installiert/aktiviert), ignoriert Nextcloud den Header und
liefert die Dateien des **Admin**-Accounts. `relative_path` findet dann die
`base_path` nicht im href, fällt auf den rohen href zurück und erzeugt
kaputte Pfade (z. B. `/remote.php/dav/files/admin/...`), die im Frontend
als normale Einträge erscheinen. Es gibt keinen Abgleich zwischen
angefordertem `effective_user` und dem tatsächlichen href-Namespace.

**Fix-Vorschlag:** Nach dem Parse prüfen, ob alle hrefs unter der erwarteten
`base_path` liegen; andernfalls `AppError` mit klarer Meldung („Impersonation
vom Server nicht unterstützt").

---

### B10 — Verbesserung (Minor): Fehler-Status bei Quota setzen ist nicht i18n-fähig

**Datei/Funktion:** `src/components/AdminPanel.vue` → `setQuota` (Zeile 124) und `createUser` (Zeile 173)

`error.value = t("quota") + ": 0"` und `t("userId") + " + " + t("password")`
sind zusammengesetzte Strings mit hartkodierten Zahlen/Trenner. Besser als
eigene i18n-Schlüssel mit Platzhaltern (wie `deleteUserConfirm`).

---

### B11 — Verbesserung (Minor): Leere Verzeichnisse werden remote nicht gelöscht; lokale Ordner ohne Dateien nie erstellt

**Datei/Funktion:** `src-tauri/src/sync.rs` → `decide` (Zeilen 278–281), `walk_local` (Zeilen 126–168), `plan_ops` (Zeilen 302–348)

Nur Dateien werden synchronisiert; Ordner nur als Eltern für Uploads
(`EnsureDir`). Wird ein lokaler Ordner leer (alle Dateien gelöscht), bleibt
der Remote-Ordner bestehen; leere lokale Ordner werden nicht hochgeladen
(auf Nextcloud ohne `.keep`-Datei ohnehin fragwürdig, aber der Remote-Ordner
sollte beim lokalen Löschen entfernt werden).

---

### B12 — CI: Inkonsistenz beim Clippy-Schwellwert zwischen Workflows

**Datei/Funktion:** `.github/workflows/ci.yml` (Zeile 60) vs. `.github/actions/checks/action.yml` (Zeile 16) und `.github/actions/lint/action.yml` (Zeile 15)

`ci.yml` ruft `cargo clippy --all-targets` **ohne** `-- -D warnings` auf,
während die wiederverwendbaren Actions und `AGENTS.md` (Zeile 34) `-D
warnings` vorschreiben. Aktuell ist der Baum sauber (verifiziert), aber künftige
Clippy-Warnungen würden in `ci.yml` grün durchlaufen und nur in `lint`/`build`
scheitern. Einheitlich `-D warnings` verwenden.

---

### B13 — CI/Security: `opencode.yml` erlaubt Code-Ausführung aus Kommentaren mit Schreibzugriff

**Datei/Funktion:** `.github/workflows/opencode.yml` (Zeilen 34–112)

Der Workflow triggert bei jedem Issue/PR-Kommentar mit `/oc` oder
`/opencode` von Nutzern mit Schreibzugriff und gibt der Agent-Session
`contents: write`, `pull-requests: write`, `issues: write` **und**
`id-token: write`. Der Kommentartext wird ungefiltert in den Prompt injiziert
(„Handle the triggering comment"). Damit kann jeder Write-Collaborator über
Prompt-Injection beliebigen Code in einer Umgebung mit Repository-Write-Rechten
ausführen. Zudem ist `id-token: write` unnötig (kein OIDC-Verbrauch) und
vergrößert die Angriffsfläche.

**Empfehlungen:** `id-token: write` entfernen; den Prompt so umformulieren,
dass der Kommentar als *Aufgabenbeschreibung*, nicht als Instruktion
behandelt wird; Rechte so eng wie möglich halten; den Diff der Agent-Änderungen
vor dem Auto-Commit auf verdächtige Muster prüfen (z. B. Secrets-Exfiltration).

---

### B14 — Robustheit: Kein CSP in `tauri.conf.json`

**Datei/Funktion:** `src-tauri/tauri.conf.json` → `app.security.csp` (Zeile 23)

`"csp": null` deaktiviert die Content Security Policy. Die App lädt nur
lokale Assets, aber Tauri v2 empfiehlt eine CSP. Ein `default-src 'self'`
(bzw. `script-src 'self'`) wäre ein sinnvoller Mindestschutz, falls je
Remote-Inhalte (z. B. SVG/Bilder aus WebDAV) gerendert werden.

---

### B15 — Verbesserung (Minor): Ungenutzte Abhängigkeit `@tauri-apps/plugin-opener`

**Datei/Funktion:** `package.json` (Zeile 16), `src-tauri/src/lib.rs` (Zeile 116)

`plugin-opener` ist registriert, wird aber im Frontend nirgends verwendet
(verifiziert per Grep). Entweder nutzen (z. B. „Im Browser öffnen" für
Share-Links) oder entfernen.

---

### B16 — Verbesserung (Minor): `ci.yml` installiert System-Dependencies doppelt

**Datei/Funktion:** `.github/workflows/ci.yml` (Zeilen 37–46) vs. `.github/actions/setup/action.yml` (Zeilen 15–27)

Der Backend-Job in `ci.yml` installiert die Linux-Abhängigkeiten selbst,
obwohl die `setup`-Action (im `lint.yml`-Pfad und `build.yml` genutzt) das
bereits tut. Auf die `setup`-Action umstellen, um Duplikation und Drift zu
vermeiden.

---

### B17 — Robustheit: `keyring`-Ausfall macht die App auf Linux ohne Secret-Service unbenutzbar

**Datei/Funktion:** `src-tauri/src/accounts.rs` → `save_token` (Zeilen 20–26), `load_accounts` (Zeilen 54–68)

Auf Linux-Desktops ohne laufenden Secret-Service (z. B. minimal Window
Manager, headless CI) schlägt `save_token` fehl → `account_add` bricht ab;
`load_accounts` überspringt stumm alle Konten ohne Token, d. h. die gesamte
Account-Liste verschwindet temporär. README erwähnt die Keychain-Persistenz,
aber ein erklärender Fehlertext bzw. ein Dokumentationshinweis für
Linux-Nutzer (gnome-keyring/kwallet nötig) fehlt.

---

### B18 — Robustheit: Reste von `.tmp`-Dateien werden nicht aufgeräumt

**Datei/Funktion:** `src-tauri/src/nextcloud/webdav.rs` → `get_file` (Zeilen 147–159), `tmp_path` (Zeilen 241–248)

Bricht ein Download nach `File::create(&tmp)` ab (Stream-Fehler), wird die
`.flutlink-<pid>.tmp`-Datei nicht gelöscht (nur im Fehlerfall von `rename`
aufgeräumt). Die Datei ist zwar versteckt und wird von `should_skip_name`
übersprungen, sammelt sich aber über die Zeit an. Beim Stream-Fehler ebenfalls
`remove_file(&tmp)` ausführen.

---

## Bereits gut umgesetzt (kein Handlungsbedarf)

- Fehler-Serialisierung `AppError` mit `code`/`message` und `From`-Impls ist
  sauber (error.rs).
- WebDAV-Multistatus-Parser hat gute Unit-Tests inkl. URL-Encoding-Fall
  (webdav.rs).
- Sync-Engine: Journal-basierte Zwei-Wege-Entscheidung ist durchdacht und
  getestet (21 Tests grün); Konfliktkopien + Eltern-Verzeichnis-Anlage
  funktionieren laut Tests.
- `Impersonate-User`-Zugriffskontrolle in `webdav_list` (Admin-Check vor dem
  Header) ist korrekt.
- Temporäre Datei + `rename` beim Download verhindert korrupte Ziele.
- Tray/Close-to-Tray-Handling und CLI-Flags sind konsistent registriert.
- Secrets werden nicht in `accounts.json` persistiert (nur Metadaten).

---

## Zusammenfassung

- **Bugs:** B1 (Account-Re-Add), B2 (System-Theme), B5 (totes Command),
  B6 (unbehandeltes Event), B7 (Doku vs. Realität), B8 (Store/Backend-Desync,
  Folge von B1)
- **Feature-Lücken:** B3 (Impersonation-Sharing), B4 (Pagination), B11
  (Ordner-Sync), B15 (opener ungenutzt)
- **Robustheit/Security:** B9, B13, B14, B17, B18
- **CI:** B12, B16
- **Doku/UX:** B10

Keine Änderungen an Anwendungscode oder Workflows vorgenommen. `todo.md` war
leer — keine bestehenden Einträge abzuhaken, keine `archived-todo.md` angelegt.

---

# Nachtrag — Lauf 2 (13.08.2026, Fokus: „Features, die bis v1 fehlen")

**Reviewer:** opencode (Agent `reviewer`, Modell deepseek-v4-flash-free)
**Datum:** 13. August 2026
**Fokusbereich:** fehlende Features/Bugs mit v1-Relevanz (Sync-Engine,
Dateioperationen, Konto-/Sync-Lifecycle, Updater, Release-Infrastruktur)

## Verifikation (ausgeführt)

| Check | Ergebnis |
| --- | --- |
| `cargo test --manifest-path src-tauri/Cargo.toml` | ✅ 36 Tests bestanden (inkl. updater- und sync-Tests) |
| `npm run build` (vue-tsc + vite) | ✅ kompiliert sauber |
| Logik-Replikation `decide()` (Sync-Engine) + `add_folder`-Duplikat-Check in `/tmp/opencode/sync_verify.rs` | ✅ Befunde V1.1 und V1.2 bestätigt (siehe unten) |

## Status der bestehenden todo.md-Einträge

**Kein einziger Eintrag aus `todo.md` ist erledigt.** Der Anwendungscode ist
seit dem letzten Review unverändert (git log: letzte Commits betreffen
Updater/Workflows). Konkret geprüft und **weiterhin offen**: F1/B1
(Konto-Identifikation nur `username`), B3 (Konfliktkopie-Namen),
B2-alt (register_user Teilfehler), F3 (Files-Race), B4-alt/B11 (Ordner-Sync),
B5-alt (statuses-Init beim Start), B6-alt (delete_token verschluckt),
B7-alt (keine target_user-Transfers), B8-alt (admin_edit_user ohne Whitelist),
B9-alt (keine Datei-Commands), B11-alt (tmp_path nicht eindeutig),
U1/U2/U3, F2 (System-Theme No-Op), F4 (doppeltes „frei"), F5 (fehlende
i18n-Keys), F6 (unbehandelte Promise-Rejection), C1/C2/C4, sowie alle
B1–B18 aus dem Abschnitt „Review vom 2026-08-13". Es wurde daher **nichts
abgehakt und nichts nach `archived-todo.md` verschoben** (Datei existiert
weiterhin nicht).

## Neue Befunde (Lauf 2)

### V1.1 — Bug: `sync_add` erlaubt kollidierende `remote_path` für gleichnamige lokale Ordner

**Datei/Funktion:** `src-tauri/src/commands.rs` → `sync_add` (Zeilen 410–416,
`remote_path: format!("/FlutLink/{}", name)`) i. V. m. `src-tauri/src/sync.rs`
→ `SyncEngine::add_folder` (Zeilen 666–679, Duplikat-Check nur auf
`account_key` + `local_path`)

Zwei lokale Ordner mit gleichem Ordnernamen, aber unterschiedlichen Pfaden
(z. B. `/home/a/Docs` und `/home/b/Docs`) werden für denselben Account beide
angenommen und beide auf `/FlutLink/Docs` gemappt. Der Duplikat-Check
vergleicht `local_path`, nicht `remote_path`. Zwei Sync-Engines schreiben
dann in denselben Remote-Ordner: Remote-Änderungen von Ordner A werden von
Ordner B als Löschung interpretiert (`decide` → `DeleteRemote`), Journal
und lokale Kopien laufen auseinander → Datenverlust möglich.

Verifiziert durch Replikation der `add_folder`-Prüflogik:
```
V1.1: add /home/b/Docs (gleicher Account, gleicher Name, anderer Pfad)
      => already-synced check = false  → BUG CONFIRMED
```

**Fix-Vorschlag:** Eindeutigen Remote-Namen ableiten (z. B. aus dem
canonical-Pfad per Slug) oder `remote_path` in die Duplikat-Prüfung
aufnehmen.

### V1.2 — Bug: Typ-Konflikt Datei ↔ Ordner wird in der Sync-Engine nicht behandelt

**Datei/Funktion:** `src-tauri/src/sync.rs` → `decide` (Zeilen 236–289),
`exec_download` (Zeilen 427–441); `walk_local` sammelt nur Dateien
(Zeilen 126–168)

Wird lokal aus einer Datei ein Ordner gleichen Namens (oder umgekehrt),
fehlt jede Behandlung. Drei bestätigte Fehlfälle (Logik-Replikation):

1. **Lokaler Ordner vs. Remote-Datei mit Journal-Eintrag**
   → `local = None` (Ordner wird von `walk_local` nicht erfasst) →
   `decide` liefert `DeleteRemote` → die Remote-Datei wird **gelöscht**,
   obwohl der Nutzer nur lokal einen Ordner angelegt hat.
2. **Lokale Datei vs. Remote-Ordner** → `decide` liefert `Skip`
   (Zeile 238–240) → die lokale Datei wird still ignoriert, der Nutzer
   verliert sie beim nächsten Sync nicht, aber sie wird nie hochgeladen.
3. **Erst-Sync, lokaler Ordner vs. Remote-Datei** → `Download` →
   `exec_download` erzeugt mit `create_dir_all` + `get_file` die Datei unter
   dem Pfad des existierenden lokalen Ordners → `File::create` schlägt fehl
   → Sync-Pass bricht mit Fehler ab.

**Fix-Vorschlag:** Typ-Wechsel erkennen und als Konflikt behandeln
(`UploadConflict` mit eindeutigem Namen bzw. Konflikt-Review), statt
`DeleteRemote`/`Skip`/abgebrochenem Download.

### V1.3 — Bug (Minor): Resume zeigt weiterhin Status „Paused" bis zum nächsten Worker-Tick

**Datei/Funktion:** `src-tauri/src/sync.rs` → `set_paused` (Zeilen 716–732)

`set_paused(folder_id, false)` setzt `status.paused = false`, aber `state`
bleibt `"paused"` — erst der nächste Pass von `run_all` (bis zu 10 s
Intervall, bzw. bis zum nächsten Trigger) setzt den Zustand auf `"idle"`.
Das Sync-Panel zeigt also nach „Fortsetzen" noch bis zu 10 s „Paused".

**Fix-Vorschlag:** Bei `paused == false` den Status sofort auf `"idle"`
setzen (oder direkt `notify_one()` + Status-Update emittieren).

### V1.4 — Feature: Sync-Ordner werden beim Konto-Entfernen nicht aufgeräumt

**Datei/Funktion:** `src-tauri/src/commands.rs` → `account_remove`
(Zeilen 241–255) i. V. m. `src-tauri/src/sync.rs` → `run_all`
(Zeilen 800–806)

`account_remove` entfernt nur das Konto. Die zugehörigen Einträge in
`sync-folders.json` bleiben erhalten und zeigen im Sync-Panel dauerhaft
`state = "error"` mit „Account is no longer connected." — bis sich derselbe
Benutzer wieder anmeldet. Es gibt weder ein automatisches Cleanup noch eine
UI-Option, verwaiste Sync-Ordner zu löschen.

**Fix-Vorschlag:** Beim Konto-Entfernen die Sync-Ordner des `account_key`
mit entfernen (Journal-Dateien inklusive) oder im UI als „verwaist"
kennzeichnen und löschbar machen.

### V1.5 — Feature: Updater verifiziert Download nicht (keine SHA-256-Prüfung, kein Auto-Check)

**Datei/Funktion:** `src-tauri/src/updater.rs` → `download_update`
(Zeilen 223–281), `check_update` (Zeilen 427–431);
`src/components/SettingsModal.vue` → `checkForUpdate` (Zeilen 95–106)

Der heruntergeladene Installer wird weder gegen die GitHub-Asset-Checksumme
geprüft noch signaturverifiziert; `asset_size` wird nur für den Fortschritt
verwendet. Zudem gibt es keinen automatischen Update-Check beim App-Start —
der Nutzer muss das Modal öffnen und manuell klicken. Für v1: Checksumme
(aus den GitHub-API-Metadaten, z. B. `digest` bei `draft:false`-Assets,
oder aus einer separaten `.sha256`-Datei im Release) prüfen und optional
einen Auto-Check beim Start mit sanftem Hinweis ergänzen.

### V1.6 — Feature (v1-Blocker): Keine Dateioperationen im Dateibrowser

**Datei/Funktion:** `src/components/FileExplorer.vue` → `open` (Zeilen 21–23)
lässt Datei-Klicks ins Leere laufen; `src/lib/ipc.ts` (keine Wrapper für
Upload/Download/Delete/Rename/Mkdir); `src-tauri/src/commands.rs`
(nur `webdav_list` und `webdav_create_share`)

Die Transfer-Helper `put_file`, `get_file`, `delete`, `make_collection`,
`ensure_collection` existieren in `src-tauri/src/nextcloud/webdav.rs`
(Zeilen 82–226), sind aber nicht als `#[tauri::command]` exponiert und im
Frontend nicht erreichbar. Der Browser ist damit reine Leseansicht —
für v1 sind Upload, Download/Öffnen, Umbenennen, Neuer Ordner und Löschen
notwendig (steht als Todo B9/U2 bereits offen; hier als v1-Blocker benannt).
Zusätzlich bleibt `webdav_create_share` ohne `target_user` (siehe B3) —
Impersonation-Sharing funktioniert damit auch nach B9 nicht.

### V1.7 — Bug (Minor): `FLUTCLOUD_URL`-Fehler wird in `OnceLock` gecacht

**Datei/Funktion:** `src-tauri/src/flutcloud.rs` → `flutcloud_url`
(Zeilen 17–28)

`OnceLock<Result<String, String>>` cached auch den **Fehlerfall**
(„FLUTCLOUD_URL is not set"). Wird die `.env`-Datei während der Laufzeit
korrigiert (oder beim ersten Aufruf vor dem Setzen der Variable abgefragt),
bleibt der Fehler bis zum App-Neustart bestehen — `account_add`,
`register_user` und `get_flutcloud_url` schlagen dauerhaft fehl.

**Fix-Vorschlag:** Nur den Erfolgswert cachen, Fehler nicht in den
`OnceLock` schreiben (bzw. `OnceLock<Option<String>>` mit Fehlerpfad, der
erneut versucht).

### V1.8 — Feature/Infrastruktur (v1-Blocker für Distribution): Builds sind nicht signiert/notarized

**Datei/Funktion:** `.github/workflows/release.yml` (Zeile 121,
`APPLE_SIGNING_IDENTITY: "-"`)

Der macOS-Build nutzt Ad-hoc-Signing (`-`), Windows hat kein Code-Signing-
Zertifikat, und es fehlt die macOS-Notarisierung. Downloads außerhalb von
GitHub (bzw. nach Gatekeeper/Defender) werden als „beschädigt"/„unbekannt"
geblockt. Für eine v1-Distribution sind (a) macOS Developer-ID-Signing +
Notarisierung und (b) ein Windows-Code-Signing-Zertifikat (oder alternativ
ein dokumentierter SmartScreen/Defender-Ausschluss für den
Early-Access-Zeitraum) erforderlich.

---

## Zusammenfassung Lauf 2

- **Neue Bugs (verifiziert):** V1.1 (remote_path-Kollision), V1.2
  (Typ-Konflikt Datei↔Ordner, 3 Fehlfälle), V1.3 (Resume-Status), V1.7
  (OnceLock-Fehler-Caching)
- **Fehlende v1-Features:** V1.4 (Sync-Cleanup), V1.5 (Update-Integrität +
  Auto-Check), V1.6 (Dateioperationen, v1-Blocker), V1.8 (Signing, v1-Blocker)
- **todo.md:** keine bestehenden Einträge erledigt → nichts abgehakt, keine
  `archived-todo.md` angelegt; neue Befunde V1.1–V1.8 als neuer datierter
  Abschnitt oben in `todo.md` ergänzt.
- Keine Änderungen an Anwendungscode oder Workflows vorgenommen.